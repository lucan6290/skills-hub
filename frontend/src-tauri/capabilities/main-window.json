{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Capabilities for the Skills Hub main window. Granular per-plugin permissions on top of core defaults.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "deep-link:default",
    "notification:default",
    "updater:default"
  ]
}
