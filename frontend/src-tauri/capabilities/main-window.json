{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal core permissions for the bundled Skills Hub main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
